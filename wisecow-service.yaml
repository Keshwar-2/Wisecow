apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
spec:
  type: NodePort
  selector:
    app: wisecow
  ports:
    - protocol: TCP
      port: 4499        # external port
      targetPort: 4499  # container port
      nodePort: 30499   # port between 30000-32767
